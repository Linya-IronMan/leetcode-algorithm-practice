#include <stdio.h>
#include <stdlib.h>
#include <assert.h>

// TODO: 修改函数签名与实现
int* ${method_name}(int* nums, int numsSize, int target, int* returnSize) {
    *returnSize = 0;
    return NULL;
}

int main() {
    // {{TEST_CASES}}
    printf("C: All tests passed!\n");
    return 0;
}
